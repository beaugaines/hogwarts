<h2>New Student</h2>
<%= form_for @student do |f| %>
  <fieldset>
    <%=  f.label :name %>
    <%=  f.text_field :name %>
    <%= f.label :house_id %>
    <%= f.collection_select(:house_id, House.all, :id, :name) %>
  </fieldset>
  <%= f.submit %>
<% end %>
